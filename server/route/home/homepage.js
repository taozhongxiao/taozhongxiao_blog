const { Otherarticle } = require('../../model/article')

// module.exports = async (req, res, next) => {
// 	let mainArticle = await Otherarticle.find({})
// 	// 渲染用户列表模块
//   // 将页面重定向到用户列表页面
//   res.status('200').send({meta: { status: 200 }, data: { mainArticle, message: '获取用户信息成功' }})
//   console.log('final')
// }

